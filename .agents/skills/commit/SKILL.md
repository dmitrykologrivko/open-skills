---
name: commit
description: Make a short commit message from the current Git diff.
---

# Commit

Run `git diff` and `git diff --cached`.

Output one short imperative line. Use simple caveman words. No body. No prefix.

If no diff, say `No diff.`

Run `git commit`.
